version: '2'
services:
  db:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=secret
      - POSTGRES_DB=todo
  web:
    image: todo
    ports:
      - 8080
    links: 
      - db:postgresql
